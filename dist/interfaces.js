"use strict";
let employee4 = {
    id: 1,
    nameE: "Bryan",
    retire: (age) => {
        if (age > 65)
            return true;
        return false;
    }
};
let employee5 = {
    id: 1,
    nameE: "Julian"
};
const p1 = 1;
const sum = (x, y) => x + y;
console.log(sum(2, 3));
const subs = (x, y) => x - y;
subs(6, 1);
const sumV2 = (x, y) => x + y;
console.log(sumV2(2, 3));
//# sourceMappingURL=interfaces.js.map